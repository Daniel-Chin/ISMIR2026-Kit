# Drafted by GPT-5

name: Refresh website mockup

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  refresh-mockup:
    uses: ./.github/workflows/refresh-website.yml
    with:
      mockup: true
    secrets:
      ZOOM_REDIRECT_ACCESS_TOKEN: ${{ secrets.ZOOM_REDIRECT_ACCESS_TOKEN }}